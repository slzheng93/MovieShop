@model List<ApplicationCore.Models.GenreModel>

    @foreach (var genre in Model)
    {
        <a asp-controller="Movies" asp-action="genres" asp-route-id="@genre.Id" class="dropdown-item">
            @genre.Name
        </a>
    }